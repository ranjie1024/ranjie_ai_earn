# ranjie_ai_earn

C 语言示例：实现车辆最高车速限制逻辑。

## 文件说明
- `vehicle_speed_limiter.c`：包含限速函数 `limit_speed`，最高车速固定为 `120 km/h`。

## 编译与运行
```bash
gcc -Wall -Wextra -pedantic vehicle_speed_limiter.c -o speed_limiter
./speed_limiter
```
